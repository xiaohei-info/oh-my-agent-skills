# Skills Directory

Each subdirectory under `skills/` is a themed bundle.

Within each bundle:
- one skill = one folder
- `SKILL.md` is the entrypoint
- support files stay local to the skill folder

If you are installing into Hermes, preserve the whole skill directory rather than copying only the markdown file.
